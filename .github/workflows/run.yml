name: Run SPAC

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Build the Docker image
        run: docker build . --file Dockerfile --tag spac

      - name: Save refresh token to file
        run: mkdir ./tokens && echo "$REFRESH_TOKEN" > ./tokens/refresh_token && id -u runner
        env:
          refresh_token: ${{ secrets.REFRESH_TOKEN }}

      - name: Echo
        run: id -g runner

      - name: Run SPAC
        run: docker run -e UID=1001 -e GID=1001 -v $(pwd)/tokens:/tokens:rw -v $(pwd)/data:/spac/data:rw --rm spac

      - name: Set refresh token secret
        run: gh secret set REFRESH_TOKEN < ./tokens/refresh_token
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
