{
  "configurations": [
    {
      "name": "Attach to process",
      "type": "node",
      "request": "attach",
      "address": "localhost",
      "port": 9229,
      "skipFiles": [
        // Node.js internal core modules
        "<node_internals>/**",

        // Ignore all dependencies (optional)
        "${workspaceFolder}/node_modules/**"
      ],
      "localRoot": "${workspaceFolder}/spotify-playlists-as-code",
      "remoteRoot": "/spotify-playlists-as-code"
    }
  ]
}
